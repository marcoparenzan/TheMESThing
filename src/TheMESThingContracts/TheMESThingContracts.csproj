<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Roslyn source generator: compiles Contracts/*.yaml into C# on every build.
         PrivateAssets="all" keeps it out of the dependency graph of projects referencing this one. -->
    <PackageReference Include="Ontly.Generators" Version="0.1.0" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <!-- mes.contract.yaml is the compilation root; the other files are includes.
         All are listed so that editing any of them triggers regeneration. -->
    <AdditionalFiles Include="Contracts\mes.contract.yaml" />
    <AdditionalFiles Include="Contracts\mes.yaml" />
    <AdditionalFiles Include="Contracts\telemetry.yaml" />
    <AdditionalFiles Include="Contracts\measures.yaml" />
  </ItemGroup>

</Project>
